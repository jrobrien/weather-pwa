# Do not run install/postinstall lifecycle scripts from dependencies.
# Cuts the main supply-chain attack vector; this project's deps use prebuilt
# platform binaries and don't need build scripts. Verify `npm run build` after
# adding a dependency in case it does.
ignore-scripts=true
